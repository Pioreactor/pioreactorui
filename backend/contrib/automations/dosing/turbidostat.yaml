---
display_name: Turbidostat
automation_name: turbidostat
description: Every minute, check if the normalized OD is above target OD. If so, exchange volume amount. For high growth cultures (for example, h⁻¹ > 1.0), try a volume larger than 2.0 mL.
fields:
  - key: duration
    default: 1
    unit: min
    label: Time between check
    disabled: True
  - key: target_od
    default: 2.0
    unit: AU
    label: Target OD
  - key: volume
    default: 0.5
    unit: mL
    label: Volume
