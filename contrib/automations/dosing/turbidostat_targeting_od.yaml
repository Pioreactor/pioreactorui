---
display_name: Turbidostat targeting OD
automation_name: turbidostat_targeting_od
description: Every half-minute, check if the OD is above target OD. If so, exchange volume amount.
fields:
  - key: duration
    default: 0.5
    unit: min
    label: Time between check
    disabled: True
  - key: target_od
    default: 0.2
    label: Target OD
    unit:
  - key: volume
    default: 0.5
    unit: mL
    label: Volume
